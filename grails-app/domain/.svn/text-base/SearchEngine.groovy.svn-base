class SearchEngine extends Content {

  static hasMany = [bookmarks: Bookmark]

  static mapping = {
    table 'SearchEngines'
  }

  static constraints = {
  }

  String toString() {
    "Bookmark with ${bookmarks?.size()} bookmarks"
  }

}